# Write a program using the built-in max() function to find the largest number in a list.

li=list(map(int,input("Enter elements into the list:").split()))

highest=max(li)
print("The largest number in the list is:",highest)
